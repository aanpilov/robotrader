<?xml version="1.0" encoding="UTF-8"?>
<features xmlns="http://karaf.apache.org/xmlns/features/v1.0.0" name="${project.groupId}">
    <repository>mvn:${project.groupId}/${project.artifactId}/${project.version}/xml/features</repository>
    
    <feature name="${project.artifactId}" version="${project.version}">
        <feature>camel-http4</feature>
        <bundle>mvn:${project.groupId}/${project.artifactId}/${project.version}</bundle>       
        <bundle>mvn:com.robotrader/core/${project.version}</bundle>       
        <bundle>wrap:mvn:eu.verdelhan/ta4j/0.9-SNAPSHOT</bundle>       
        <bundle>mvn:joda-time/joda-time/2.3</bundle>       
    </feature>

</features>
